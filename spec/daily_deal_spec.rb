require 'spec_helper'

describe DailyDeal do
  it 'has a version number' do
    expect(DailyDeal::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
